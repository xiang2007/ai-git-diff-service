[project]
name = "ai-git-diff-service"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi[standard]>=0.141.1",
    "unidiff>=1.0.0",
]

[tool.fastapi]
entrypoint = "src.backend:app"
